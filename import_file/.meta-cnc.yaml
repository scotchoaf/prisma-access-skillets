name: import_prisma_access_config_file_v90
# label used for menu selection
label: Import the Prisma Access Generated Config File

description: |
  This skillet imports the full prisma access config file to be used by the load config partial skillet

type: python3

# Labels allow grouping and type specific options and are generally only used in advanced cases
labels:
    collection:
      - Prisma Access

# variables define the things an operator may customize in this skillet. Things like DNS servers, NTP addresses, etc
# may be customized for each deployment. Each variable will be rendered as a form field in the panhandler application
variables:
  - name: TARGET_IP
    description: NGFW IP or Hostname
    default: 192.168.55.10
    type_hint: hidden
  - name: TARGET_USERNAME
    description: NGFW Username
    default: admin
    type_hint: hidden
  - name: TARGET_PASSWORD
    description: NGFW Password
    default:
    type_hint: hidden
  - name: conf_filename
    description: name of file used by load config partial
    default: prisma_access_full_config.xml
    type_hint: text

# Snippets is an ordered list of configuration xml fragments that will be pushed to the PAN-OS NGFW. The xpath
# determines where in the configuration hierarchy the xml fragment will be set. 'file' indicates the name of the file
# to load and parse. Jinja2 style variables will be variable interpolated using the values of the 'variables' defined
# in the 'variables' section.
snippets:
  - name: import_file
    file: import_file.py